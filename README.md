# پینوا (Pinova)

پینوا افزونه‌ای رایگان و متن‌باز برای ورود و عضویت کاربران وردپرس با رمز یک‌بارمصرف (OTP)، ایمیل، شمارهٔ موبایل، نام کاربری و رمز عبور است. افزونه با ووکامرس و چند سرویس پیامکی و ابزار رایج وردپرس یکپارچه می‌شود.

- صفحهٔ رسمی: [wordpress.org/plugins/pinova](https://wordpress.org/plugins/pinova/)
- مخزن توسعه: [github.com/vahid162/pinova](https://github.com/vahid162/pinova)
- تاریخچهٔ نسخه‌ها: [CHANGELOG.md](CHANGELOG.md)
- مجوز: GPLv3

> **وضعیت فعلی RC11 در ۲۰۲۶-۰۹-۲۱ — نصب نکنید:** [`v1.2.6-rc11`](https://github.com/vahid162/pinova/releases/tag/v1.2.6-rc11) از کامیت ادغام‌شدهٔ `2bc95b061439dd2d768051cb721e16a7002c10a9` منتشر شده است. اجرای دقیق PR30، اجرای جداگانهٔ `main`، اجرای شاخهٔ انتشار و publisher همگی با ۱۶ job موفق بودند. Release شمارهٔ `392490579` یک پیش‌انتشار غیر draft با `immutable: true` است؛ بررسی‌های attestation انتشار و هر دو asset موفق شدند و بازدانلود مستقل فایل نصب، SHA-256 برابر `111656689dfb823e94d9911b9327d9136292f0be9fcffeb532058ac9b22272fe` را تأیید کرد. با این حال، بررسی پس از انتشار یک ایراد P1 باقی‌مانده را تأیید کرد: وقتی هدرها قبلاً ارسال شده باشند، وردپرس ممکن است از `wp_safe_redirect()` مقدار موفق برگرداند، در حالی که هدر `Location` قابل ارسال نیست. بنابراین RC11 دست‌نخورده حفظ می‌شود اما برای نصب مناسب نیست و باید با RC12 بازبینی‌شده جایگزین شود. [گزارش انتشار و جایگزینی RC11](.agents/reviews/v1.2.6-rc11-release.md) جزئیات کامل را ثبت می‌کند. این انتشار stable/latest نیست و نصب روی سایت، تغییر تنظیمات یا فعال‌سازی مسدودسازی مسیر بومی را اعلام یا مجاز نمی‌کند.

> **وضعیت ثبت‌شده در ۲۰۲۶-۰۹-۱۹، پیش از ادغام مستندات PR27:** آخرین پیش‌انتشار منتشرشده در این بررسی، [`v1.2.6-rc9`](https://github.com/vahid162/pinova/releases/tag/v1.2.6-rc9) با مبنای `1d39ec04d5f6293d4ea3511480980fbc8912938d` است. API برای Release شمارهٔ `391638137` مقادیر `draft: false`، `prerelease: true` و `immutable: true` را گزارش می‌کند. مقدار SHA-256 گزارش‌شدهٔ فایل نصب در API برابر `4c34353127cfed47cc0dfd42f042debe67727279370afbfe34e756e6f176ed5d` است؛ این مشاهده، بازدانلود مستقل یا بررسی تازهٔ attestation نیست. اصلاحات PR25 و PR26 در RC9 منتشر شده‌اند. نسخهٔ نصب‌شده روی سایت در این مرحله بررسی نشده است. برای وضعیت جدیدتر، Releases و گزارش انتشار مربوط را بخوانید؛ این بند یک snapshot است، نه وضعیت زندهٔ نصب.

> **snapshot تاریخی اصلاح منتشرنشدهٔ PR27:** در شاخهٔ `fix/checkout-modal-corner-controls`، ضربدر در گوشهٔ بالا-چپ خود کارت قرار دارد و قواعد جای‌گذاری ضربدر و بازگشت فقط در محدودهٔ پاپ‌آپ تقویت شده‌اند. لوگو، فرم‌ها، منطق ورود و طراحی صفحهٔ ورود مستقل تغییر نکرده‌اند. شناسهٔ دارایی مشترک `.5` است. آزمون‌های فوکوس با رفتار واقعی رابط همگام شده‌اند؛ پنج خرابی کنترل‌شده و پاک‌سازی محدود محیط CI نیز بررسی می‌شوند. [گزارش PR27](.agents/reviews/checkout-modal-controls-pr27.md) نتایج تأییدشدهٔ کامیت `1423de55…` را ثبت می‌کند؛ کامیت مستنداتی بعدی باید CI و بازبینی خودش را بگذراند. ادغام و انتشار نامزد بعدی در این snapshot هنوز انجام نشده بودند. RC9 نباید بازنویسی شود و نصب روی سایت مجوز جدا می‌خواهد.

> **سابقهٔ RC8:** [`v1.2.6-rc8`](https://github.com/vahid162/pinova/releases/tag/v1.2.6-rc8) از commit ادغام‌شدهٔ `9d7f70b` منتشر شد. Release آن `immutable: true` بود؛ کنترل‌های CI و attestation موفق بودند و دو بازدانلود مستقل فایل نصب، SHA-256 برابر `343d92e7e6919a045ba530cbe73910271bcc0dd7596a7d6a05f7ef6bf94c698d` را تأیید کردند. اعلام نصب RC8، گزارش تاریخی بهره‌بردار است و نسخهٔ نصب‌شدهٔ امروز را تعیین نمی‌کند. RC1 تا RC9 بدون جابه‌جایی یا بازنویسی حفظ می‌شوند. انتشار GitHub مجوز نصب، تغییر تنظیمات یا فعال‌سازی مسدودسازی مسیر بومی نیست.

> **checkpoint تاریخی پس از ادغام PR27:** PR27 و مستندات آن در `04535ac308e85497f453305c2133facf244d4a54` ادغام شدند؛ بندهای بالاتر، snapshot پیش از این ادغام هستند. اجرای جداگانهٔ `main` به شمارهٔ `35426974523` یک ایراد واقعی بازگشت فوکوس را پیدا کرد و انتشار متوقف شد. اصلاح محدود بعدی در [PR28](https://github.com/vahid162/pinova/pull/28) انجام شد. در زمان این checkpoint، RC9 آخرین نسخهٔ منتشرشده بود. [گزارش ایراد و اصلاح فوکوس](.agents/reviews/modal-focus-lifecycle.md) را برای سابقه بخوانید و برای وضعیت جاری به هشدار RC11 مراجعه کنید. این بند تاریخی به‌تنهایی نصب روی سایت را اعلام نمی‌کند.

## امکانات خط ۱.۲.x

- ورود یا ثبت‌نام با OTP از طریق کانال‌های پیکربندی‌شده
- ورود با رمز از طریق pipeline استاندارد وردپرس و سازگار با hookهای امنیتی و 2FA
- تشخیص کاربر با نام کاربری، ایمیل یا موبایل legacy
- سیاست نقش‌محور؛ مدیر از مسیر خصوصی پینوا وارد همان pipeline بومی وردپرس، Wordfence و 2FA می‌شود و مسیر مستقیم `wp-login.php` پس از آزمون قابل مسدودسازی است
- ذخیرهٔ موبایل جدید در `pinova_mobile` بدون بازنویسی `wp_users.user_login`
- توقف امن تطبیق موبایل وقتی یک مقدار به چند User ID برسد
- یکپارچگی حساب و checkout ووکامرس، اعتبارسنجی موبایل روی دادهٔ پردازش‌شدهٔ خود ووکامرس و اعلام سازگاری HPOS
- پنجرهٔ ورود checkout همسان با رابط حساب، با بازشدن فقط از اقدام صریح ورود یا تعارض مشخص حساب؛ خطاهای عادی checkout درون فرم باقی می‌مانند، فیلد موبایل نامعتبر را متمرکز می‌کنند و پنجره حتی هنگام انتظار برای پاسخ قابل بستن است
- تفکیک هدف OTP میان ورود/ثبت‌نام و بازیابی رمز عبور؛ کد فعال فقط در همان جریان دوباره استفاده و تأیید می‌شود
- عنوان و متن ایمیل OTP متناسب با هدف ورود، ثبت‌نام یا بازیابی رمز عبور است؛ جریان‌های هم‌زمان مستقل می‌مانند و هیچ نسخه‌ای از کد برای مدیر ارسال نمی‌شود
- ورود و خروج با مقصد بازگشت خالی به fallback امن هدایت می‌شوند؛ خروج پینوا از مسیر canonical دارای slash انجام می‌شود، nonce نامعتبر پاسخ ۴۰۳ می‌گیرد و شکست redirect به‌جای پاسخ سفید، صفحهٔ کنترل‌شده با پیوند امن و رخداد privacy-safe می‌دهد
- rate limit اتمیک و Blocked List نوع‌دار برای موبایل، ایمیل، نام کاربری و IP با مسدودی موقت/دائمی و trusted proxy صریح
- خروجی Excel و VCF با capability، nonce، escaping و محدودیت ردیف
- REST response استاندارد با envelope سازگار `{success,message,data}`
- نمایش پیام معتبر خطاهای REST در رابط، همراه با زمان انتظار rate limit و Correlation ID قابل‌استفاده برای عیب‌یابی
- صفحهٔ ورود مستقل، واکنش‌گرا و RTL در یک کارت سادهٔ مرکزی با لوگوی دقیقاً وسط، محتوای عمودی متعادل در موبایل، ورودی LTR رمز عبور و پیوندهای جایگزین خوانا، همراه با پالت نارنجی/کرم برند، فرم‌های معنایی، کنترل کامل صفحه‌کلید، مسیر امن بدون جاوااسکریپت و نمایش مستقیم پیام Blocked List
- مرحلهٔ OTP با ترتیب روشن «کد ← تأیید ← ارسال دوباره ← روش جایگزین»، ویرایش صریح شناسه و خانه‌های بصری پویا روی یک ورودی واقعی با حفظ paste، autofill و طول تنظیم‌شدهٔ کد
- رفتار دسترس‌پذیر حساب با فونت Yekan Bakh قطعی، خطای زنده، فوکوس نخستین کنترل نامعتبر، انتقال فوکوس به تیتر به‌جای ورودی، قفل درخواست تکراری، محتوای inert هنگام انتظار و پشتیبانی safe area

نسخهٔ ۱.۲.۳ برای جلوگیری از تخریب بیشتر Identity طراحی شده است، اما چند حساب موجود را خودکار merge نمی‌کند. Identity Resolver، audit، migration، merge و rollback به خط توسعهٔ جداگانهٔ ۱.۳ تعلق دارند و هنوز ویژگی منتشرشدهٔ ۱.۲.۳ نیستند.

## گزارش‌گیری ساخت‌یافته در ۱.۲.۴ و اصلاحات ۱.۲.۵

پینوا رخدادهای عملیاتی مهم ورود، OTP، ارسال کانال، تعارض موبایل، rate limit، مسدودی‌ها و خروجی کاربران را با event code ثابت ثبت می‌کند. این زیرساخت دائمی است، اما رکوردهای آن موقت‌اند و به‌طور پیش‌فرض بعد از ۱۴ روز به‌صورت batch حذف می‌شوند.

- ذخیره در جدول مستقل `wp_pinova_logs` (با prefix واقعی سایت) و نمایش در **پینوا ← گزارش‌ها**
- سطح پیش‌فرض `warning`؛ سطح‌های `info` و `notice` از تنظیمات قابل انتخاب‌اند
- Debug فقط در پنجرهٔ تشخیصی ۱۵ دقیقه، یک ساعت یا ۲۴ ساعت و با انقضای خودکار
- `X-Pinova-Correlation-ID` روی پاسخ‌های REST برای تطبیق خطای کاربر با رخداد سرور
- context مبتنی بر allowlist؛ ایمیل، موبایل، username، IP، OTP، password، token، cookie، reset key، متن exception و stack trace ذخیره نمی‌شوند
- شناسه‌های لازم برای مقایسه با HMAC کلیددار و کوتاه‌شده fingerprint می‌شوند؛ User ID ممکن است برای عیب‌یابی داخلی ثبت شود
- fallback امن به logger ووکامرس و سپس PHP error log فقط وقتی جدول اختصاصی در دسترس نیست
- جلوگیری از log amplification؛ rate limit فقط لحظهٔ ورود به وضعیت محدودشده را در هر window ثبت می‌کند
- شکست نادر redirect خروج با `auth.redirect_failed` و فقط کد عملیات/علت، وضعیت header و HTTP status ثبت می‌شود؛ URL، nonce، ایمیل و token وارد گزارش نمی‌شوند
- نتیجهٔ تست مدیریتی پیامک به‌عنوان audit ثبت می‌شود و حتی با حداقل سطح `error` نیز قابل مشاهده است
- صفحهٔ گزارش‌ها وجود جدول و حداقل سطح فعال را نمایش می‌دهد؛ خالی بودن صفحه با توجه به فیلتر و سطح ثبت توضیح داده می‌شود

Retention بین ۱ تا ۹۰ روز محدود است و مدیر می‌تواند گزارش‌های موجود را با nonce از صفحهٔ مدیریت پاک کند. پاک‌سازی نیز یک رخداد مدیریتی تازه ثبت می‌کند. جزئیات قرارداد توسعه در `.agents/skills/pinova-development/references/logging.md` است.

اگر صفحهٔ گزارش‌ها خالی است، ابتدا پیام وضعیت همان صفحه را بررسی کنید. سطح `error` رخدادهای `info`، `notice` و `warning` را عمداً ذخیره نمی‌کند؛ این رفتار به معنی خرابی جدول یا UI نیست. در ۱.۲.۵ تست مدیریتی پیامک استثنا است و نتیجهٔ موفق یا ناموفق آن همیشه به‌شکل privacy-safe ثبت می‌شود.

گزارش ساخت‌یافتهٔ پینوا جایگزین logهای WordPress، WooCommerce، PHP-FPM/PHP و وب‌سرور نیست. ممکن است eventهای پینوا کاملاً سالم باشند اما هشدار lifecycle یا deprecation فقط در یکی از آن streamها دیده شود؛ در عیب‌یابی سازگاری باید آن‌ها با زمان و Correlation ID درخواست تطبیق داده شوند. پینوا عمداً همهٔ رخدادهای global از نوع `doing_it_wrong` را در جدول خود کپی نمی‌کند، چون منشأ آن می‌تواند افزونه‌ای دیگر باشد و درخواست عمومی تکراری باعث log amplification شود.

## نیازمندی‌ها

- WordPress: حداقل ۶.۸
- PHP: حداقل ۸.۱
- WooCommerce: حداقل ۷.۶ برای قابلیت‌های ووکامرس
- Docker: فقط برای محیط توسعه و تست integration
- Composer: نسخهٔ دقیق ۲.۱۰.۳ برای ساخت بستهٔ Release

نسخه‌های دقیق بررسی‌شده در CI را از `.github/workflows/quality.yml` ببینید. هر ترکیب WordPress و WooCommerce باید با محدودیت‌های رسمی خود آن نسخه‌ها سازگار باشد.

## نصب

### WordPress.org

در پیشخوان وردپرس وارد **افزونه‌ها ← افزودن افزونه تازه** شوید، «Pinova» را جست‌وجو و نسخهٔ پایدار منتشرشده را نصب کنید.

### ZIP منتشرشده در GitHub

از بخش [GitHub Releases](https://github.com/vahid162/pinova/releases) فایل `pinova-<version>.zip` را دانلود کنید. سپس در **افزونه‌ها ← افزودن افزونه تازه ← بارگذاری افزونه** همان فایل را انتخاب کنید. ZIP صحیح یک پوشهٔ اصلی با نام `pinova/` دارد و ZIP خودکار Source code جایگزین artifact نصب‌شدنی نیست.

اگر پینوا از قبل نصب است، پیش از جایگزینی فایل‌ها از دیتابیس و افزونه بکاپ بگیرید. Pre-release را ابتدا روی staging یا با برنامهٔ دقیق canary بررسی کنید. انتشار در GitHub به‌تنهایی مجوز نصب یا migration روی production نیست.

### ایمن‌سازی مسیر ورود مدیر

پینوا در **پینوا ← تنظیمات ← پیشرفته** یک مسیر خصوصی برای ورود مدیر نشان می‌دهد. این مسیر فقط نقش‌های تنظیم‌شده برای ورود بومی را می‌پذیرد و همچنان فایل و hookهای بومی ورود وردپرس را اجرا می‌کند؛ بنابراین Wordfence، 2FA، passkey و سیاست نقش‌ها دور زده نمی‌شوند.

برای جلوگیری از قفل‌شدن مدیریت، مسدودسازی مسیر اصلی در ارتقا خودکار فعال نمی‌شود و صرف وجود یک مسیر محاسبه‌شده نیز برای فعال‌سازی کافی نیست:

1. یک مسیر خصوصی معتبر را صریحاً در تنظیمات ذخیره و URL را در محل امن نگه دارید.
2. در یک پنجرهٔ خصوصی مرورگر از همان مسیر، با حسابی که `manage_options` دارد، ورود بومی موفق انجام دهید. این ورود یک مجوز یک‌بارمصرف ۳۰ دقیقه‌ای وابسته به همان مسیر، User ID و نسخهٔ افزونه ایجاد می‌کند.
3. پیش از انقضای آن مجوز، گزینهٔ «مسدودسازی مسیر اصلی وردپرس» را فعال کنید. ذخیرهٔ تنظیمات مجوز را مصرف و رکورد فعال‌سازی پایدار را ایجاد می‌کند؛ بدون هر دو مورد، مسدودسازی در runtime فعال نمی‌شود.
4. در یک نشست جدا تأیید کنید مسیر خصوصی کار می‌کند و ورود، ثبت‌نام و بازیابی حساب از `wp-login.php` پاسخ 404 می‌گیرد.

غیرفعال‌کردن گزینه یا تغییر مسیر خصوصی، رکورد فعال‌سازی و هر مجوز باقی‌مانده را پاک می‌کند و برای فعال‌سازی دوباره ورود خصوصی تازه لازم است. عملیات غیرورودی هسته فعال می‌مانند: `action=postpass` برای نوشته‌های محافظت‌شده، `action=logout` با nonce وردپرس برای خروج، `action=confirmaction` با کلید یک‌بارمصرف برای درخواست‌های حریم خصوصی، `action=confirm_admin_email` برای تأیید ایمیل مدیر و `action=exit_recovery_mode` با nonce برای خروج از Recovery Mode. reauthentication اجباری نقش‌های عادی در فرم پینوا نشست قبلی را منقضی می‌کند؛ نقش‌های ورود بومی برای این کار به مسیر خصوصی و pipeline اصلی وردپرس می‌روند. برای بازیابی اضطراری می‌توان موقتاً `PINOVA_BLOCK_NATIVE_LOGIN` را در `wp-config.php` برابر `false` قرار داد یا گزینه را با WP-CLI غیرفعال کرد. مسیر خصوصی، HMAC آن و مجوز فعال‌سازی در گزارش عمومی یا REST نمایش داده نمی‌شوند. مسیر خصوصی جای رمز قوی یا 2FA نیست و نباید در صفحهٔ عمومی سایت منتشر شود.

## شناسه‌های یک کاربر در ۱.۲.۳

وردپرس، User ID را هویت اصلی حساب در نظر می‌گیرد. پینوا در ۱.۲.۳ می‌تواند نام کاربری، ایمیل و موبایل legacy را به همان ID resolve کند، مشروط بر اینکه داده‌ها واقعاً روی همان حساب باشند و تعارضی وجود نداشته باشد.

مثلاً این سه مقدار فقط وقتی یک حساب محسوب می‌شوند که همگی به یک User ID واحد متصل باشند:

```text
sample_user
user@example.test
09120000000
```

قواعد ایمنی:

- `user_login` موجود با ورود OTP یا ویرایش موبایل تغییر نمی‌کند.
- موبایل معتبرِ ذخیره‌شده در meta فیزیکی `pinova_mobile` بر موبایل legacy اولویت دارد.
- بعد از ذخیرهٔ یک موبایل جدید، موبایل قدیمیِ موجود در username یا متاهای legacy به‌عنوان alias موبایلی پینوا استفاده نمی‌شود.
- اگر یک موبایل به چند User ID برسد، ورود با آن موبایل fail-closed می‌شود؛ «اولین ID» انتخاب نمی‌شود.
- اتصال و merge چند حساب جدا نیازمند workflow کنترل‌شدهٔ ۱.۳، انتخاب دستی حساب اصلی و انتقال امن مالکیت‌هاست.

## ساختار مخزن

```text
pinova.php                         bootstrap و metadata افزونه
src/API/                          endpointهای REST
src/Services/                     کاربر، OTP، امنیت، rate limit و کانال‌ها
src/Logging/                      logger، context امن، persistence و retention
src/Admin/Logs.php                نمایش و پاک‌سازی کنترل‌شدهٔ گزارش‌ها
src/Objects/                      Identifier و نرمال‌سازی موبایل
src/Integrations/Wordpress/       پروفایل، فهرست کاربران و export
src/Integrations/Woocommerce/     حساب، checkout و ساخت محدود مشتری
templates/                        قالب‌های ورود
assets/                           CSS، JavaScript، فونت و تصویر
tests/                            تست‌های unit، integration و مرورگر
tools/                            ابزارهای wp-env، سنجش حساسیت آزمون، پاک‌سازی CI و ساخت ZIP
CHANGELOG.md                      تاریخچهٔ مستقل و همگام با readme.txt
.agents/reviews/                  گزارش‌های مستند بررسی و انتشار
.agents/skills/pinova-development راهنمای تخصصی کار روی مخزن
AGENTS.md                         نقطهٔ شروع عامل‌های کدنویسی
```

## محیط توسعهٔ ایزوله

پیش از نصب dependency یا اجرای هر ابزار کیفیت، مشخص کنید checkout از نظر CPU/RAM/swap/I/O نیز از production و tenantهای زنده جداست. worktree یا کانتینر جدا به‌تنهایی این جداسازی را ایجاد نمی‌کند؛ روی host مشترک حتی `composer install`، full matrix، PHPStan/Composer سنگین و build انتشار را اجرا نکنید و از GitHub Actions یا host توسعهٔ دارای منابع جدا استفاده کنید.

```bash
git clone https://github.com/vahid162/pinova.git
cd pinova
composer install --no-interaction --prefer-dist
npm install --ignore-scripts
```

`composer.lock` در مخزن ثبت شده است. در snapshot این مرحله، `package-lock.json` ثبت نشده و فقط نسخه‌های مستقیم Playwright و `wp-env` در `package.json` ثابت هستند. این دو وضعیت معادل نیستند؛ این توضیح مجوز تغییر وابستگی‌ها نیست.

تست‌های integration با `@wordpress/env` در کانتینرهای جدا اجرا می‌شوند و نباید به فایل یا دیتابیس production متصل شوند. worktree، کانتینر، volume و دیتابیس آزمایشی موجود متعلق به همان task است؛ task تازه باید محیطی با نام یکتا بسازد و بدون مجوز صریح محیط قبلی را stop، update، prune یا حذف نکند.

## کنترل کیفیت

```bash
php tools/check-changelog-sync.php
npm run test:js
composer lint
composer test
composer phpstan
composer phpcs
composer audit
```

برای integration و HPOS:

```bash
npm run env:configure
npm run env:start -- --update
npx wp-env run tests-cli sudo env PHP_INI_DIR=/usr/local/etc/php docker-php-ext-install pdo_mysql
PINOVA_TEST_HPOS=no npm run test:integration
PINOVA_TEST_HPOS=yes npm run test:integration
npm run env:stop
```

با `WP_VERSION` و `WC_VERSION` می‌توان زوج سازگاری را پیش از configure انتخاب کرد. نصب `pdo_mysql` فقط داخل کانتینر disposable انجام می‌شود و چیزی را روی PHP سیستم یا production تغییر نمی‌دهد. تست‌ها فقط در محیط disposable اجرا می‌شوند.

آزمون مرورگر با `npm run test:browser` و سنجش حساسیت آن با `node tools/check-modal-test-sensitivity.mjs` اجرا می‌شود. ابتدا پیش‌نیازهای کامل محیط مرورگر، شامل PDO در کانتینر وب و CLI و آماده‌سازی مسیر ورود، باید انجام شوند. ترتیب دقیق در [مرجع کیفیت و انتشار](.agents/skills/pinova-development/references/quality-and-release.md) و [workflow واقعی](.github/workflows/quality.yml) ثبت شده است؛ فرمان‌های بالا به‌تنهایی محیط مرورگر را کامل آماده نمی‌کنند.

آزمون فوکوس باید منتظر رفتار خود افزونه بماند، نه اینکه با فرمان آزمون آن رفتار را بسازد. آزمون حساسیت، سه اجرای سالم، پنج خرابی مستقل و یک اجرای سالم نهایی را بررسی می‌کند. هر خرابی باید همان assertion مربوط را بشکند؛ خطای محیط یا شکست نامرتبط، تشخیص درست خرابی نیست. ماتریس ۱۲۰حالته، چیدمان و فوکوس مرحله‌های نمایشی را می‌سنجد؛ ۱۲۰ ورود واقعی نیست و پوشش همهٔ اشکالات افزونه را ثابت نمی‌کند.

پاک‌سازی `tools/cleanup-browser-env.sh` فقط برای پروژهٔ همان اجرای GitHub CI است و روی رایانهٔ شخصی نباید با متغیرهای جعلی CI اجرا شود. توقف محیط محلی، داده‌های آن را نگه می‌دارد؛ با حذف تأییدشده فرق دارد. گزارش‌های مرورگر در CI هفت روز نگهداری می‌شوند. خلاصهٔ کوچک، بدون اطلاعات حساس و دارای شناسهٔ دقیق اجرا در `.agents/reviews/` ثبت می‌شود. موفقیت CI نیز جای پذیرش موبایل واقعی و staging را نمی‌گیرد.

## ساخت ZIP قابل نصب

```bash
composer build
```

خروجی در `.build/pinova-<version>.zip` ساخته می‌شود. build فایل‌های توسعه مانند `.git`، `.github`، `.agents`، `AGENTS.md`، `README.md`، `CHANGELOG.md`، `node_modules`، تست‌ها و ابزارها را حذف و dependencyهای production را بسته‌بندی می‌کند. تاریخچهٔ همگام‌شده از طریق `readme.txt` داخل بسته باقی می‌ماند.

```bash
unzip -tq .build/pinova-<version>.zip
unzip -Z1 .build/pinova-<version>.zip
sha256sum .build/pinova-<version>.zip
```

برای release، ZIP از exact tag با Composer 2.10.3 دو بار ساخته می‌شود و SHA-256 هر دو build باید یکسان باشد. اسکریپت build نسخهٔ دیگری از Composer را رد، timezone را روی UTC تثبیت و permission پوشه‌ها/فایل‌ها را به 0755/0644 نرمال می‌کند تا فایل‌های تولیدی Composer، timestampها و modeهای ZIP بین محیط‌ها متفاوت نشوند. build همچنین بررسی می‌کند bootstrap اتصال دیتابیس در `vendor/composer/autoload_files.php` حاضر باشد. سپس API انتشار باید `immutable: true` گزارش کند و هر دو asset منتشرشده دوباره دانلود، با خروجی build مقایسه و همراه attestation گیت‌هاب راستی‌آزمایی شوند.

اگر Composer سراسری نسخهٔ دیگری دارد، PHAR نسخهٔ پین‌شده را صریحاً به build بدهید:

```bash
COMPOSER_PHAR=/path/to/composer-2.10.3.phar bash tools/build.sh
```

پیش‌انتشار GitHub فقط از branch کنترل‌شده‌ای مانند `publish/vX.Y.Z-rcN` با شمارهٔ استفاده‌نشده انجام می‌شود. این branch باید از commit دقیق، بازبینی‌شده و سبزِ `main` ساخته شود و مالک مخزن پیش‌تر فعال‌بودن Release Immutability را تأیید کرده باشد. workflow فقط از `GITHUB_TOKEN` همان job استفاده می‌کند و برای انتشار به secret مدیریتی یا Ruleset وابسته نیست. سپس دسترسی SHA از `main` و نسخه را بررسی می‌کند، tag حاشیه‌نویسی‌شده می‌سازد، build را دوبار مقایسه می‌کند، ZIP و checksum را با توالی صریح draft/upload/publish منتشر می‌کند و بلافاصله `immutable: true`، هر دو asset و attestation آن‌ها را دوباره راستی‌آزمایی می‌کند. اجرای مجدد فقط draft منتشرنشده‌ای را ادامه می‌دهد که نویسندهٔ ثبت‌شدهٔ آن `github-actions[bot]` باشد و نام، commit هدف، متن شناسنامه و فهرست assetهای مجاز آن دقیقاً متعلق به همین انتشار باشد؛ draft ناشناس و هر Release منتشرشده بدون تغییر باقی می‌مانند. اگر Release برخلاف انتظار mutable باشد، workflow شکست می‌خورد، همان tag و Release دست‌نخورده می‌ماند و پس از اصلاح تنظیم مخزن باید RC تازه‌ای منتشر شود.

## فرایند اصلاح باگ

1. نسخه و ref دقیق مشکل مشخص می‌شود.
2. مشکل در worktree و محیط ایزوله بازتولید می‌شود؛ production محل توسعه نیست.
3. در صورت امکان regression test نوشته می‌شود.
4. کوچک‌ترین اصلاح سازگار با قواعد امنیت و Identity انجام می‌شود.
5. تست هدفمند و سپس suite متناسب اجرا می‌شود.
6. Skill، AGENTS، README، `CHANGELOG.md` و بخش Changelog در `readme.txt` در همان change set به‌روز می‌شوند.
7. Pull Request ساخته و GitHub Actions بررسی می‌شود.
8. پس از مجوز ادغام، merge و CI کامیت ادغام‌شده بررسی می‌شوند؛ انتشار tag و ZIP جدید نیز به مجوز و کنترل‌های انتشار نیاز دارد. tag منتشرشده جابه‌جا نمی‌شود.
9. نصب production فقط با مجوز جدا، بکاپ، rollback و آزمون پس از نصب انجام می‌شود.

در هر تغییر مؤثر بر افزونه باید Skill مخزن هم بازبینی شود:

```bash
bash .agents/skills/pinova-development/scripts/check-skill-sync.sh --working-tree
```

این کنترل، مشارکت فایل Skill در تغییرات را می‌سنجد؛ صحت معنایی متن، وضعیت انتشار و پیوندهای داخلی باید جداگانه بررسی شوند.

## نقشهٔ راه Identity 1.3

خط توسعهٔ ۱.۳ برای اتصال چند identifier به یک User ID و رسیدگی کنترل‌شده به چند User ID طراحی شده است. اهداف آن شامل جدول identity یکتا، audit و dry-run، ثبت conflict، merge دستی با journal، rollback محدود به تغییرات همان run، پشتیبانی WordPress Core/WooCommerce و توقف apply روی Multisite است.

هیچ conflict چندحسابی نباید خودکار merge شود. رمز، نقش، capability، session، application password و دادهٔ افزونه‌های امنیتی حساب مبدأ نباید به حساب مقصد merge شوند. این قابلیت‌ها پیش از انتشار عمومی به staging، آزمون migration/rollback و canary جدا نیاز دارند.

## امنیت و production

- OTP، password، token، reset key و شناسهٔ کامل کاربر را log نکنید.
- فقط event code ثابت و context allowlist‌شده ثبت کنید؛ متن exception و trace نیز نباید وارد log شود.
- redirect خارجی باید با `wp_validate_redirect()` و host مجاز محدود شود.
- فقط `REMOTE_ADDR` پیش‌فرض معتبر است؛ proxy header به trusted CIDR نیاز دارد.
- migration قدیمی نباید ستون، index یا دادهٔ جدول‌های core را تغییر دهد.
- توسعه و تست خودکار نباید در `<production-wordpress-root>/wp-content/plugins/pinova` انجام شود؛ مسیر واقعی production نباید در مستندات عمومی hard-code شود.
- RC باید قبل از stable/latest شدن، staging و canary مورد توافق را بگذراند.

## راهنمای AI و مشارکت‌کنندگان

کار را از [AGENTS.md](AGENTS.md) و [.agents/skills/pinova-development/SKILL.md](.agents/skills/pinova-development/SKILL.md) شروع کنید. Skill باید با هر تغییر runtime، schema، dependency، تست، tooling، CI، command، integration یا release به‌صورت معنادار به‌روز شود. تاریخچهٔ نسخه‌ها نیز در [CHANGELOG.md](CHANGELOG.md) و بخش Changelog فایل `readme.txt` نگهداری می‌شود و `php tools/check-changelog-sync.php` از ناسازگاری آن‌ها جلوگیری می‌کند.

برای مشارکت انسانی نیز جریان توصیه‌شده شامل شرح بازتولید، branch محدود، regression test، اصلاح، کنترل کیفیت، مستندسازی و Pull Request است.

## مجوز

پینوا تحت [GNU General Public License v3](https://www.gnu.org/licenses/gpl-3.0.html) منتشر می‌شود.
